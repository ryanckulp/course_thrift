<%= render partial: "shared/heading", locals: { title: 'Browse', description: 'Sort courses by category or price.' } %>

<div class="bg-white">
  <div class="mx-auto max-w-2xl px-4 pt-8 pb-16 sm:px-6 sm:pb-24 sm:pt-12 lg:max-w-7xl lg:px-8">
    <div class="flex items-center justify-between space-x-4">
      <!-- TODO: insert dropdowns for categories / sorting -->
    </div>
    <div class="mt-6 grid grid-cols-1 gap-x-8 gap-y-8 sm:grid-cols-2 sm:gap-y-10 lg:grid-cols-3">
      <%= render @courses %>
    </div>
  </div>
</div>
